WORDPRESS_DATABASE_NAME="${WORDPRESS_DATABASE_NAME:-mariadb}" 
WORDPRESS_DATABASE_HOST="${WORDPRESS_DATABASE_HOST:-localhost}" 
WORDPRESS_DATABASE_USER="${WORDPRESS_DATABASE_USER:-userdb}" 
WORDPRESS_TABLE_PREFIX="${WORDPRESS_TABLE_PREFIX:-wp_}" 
WORDPRESS_USERNAME="${WORDPRESS_USERNAME:-user}" 
WORDPRESS_PASSWORD="${WORDPRESS_PASSWORD:-silo}" 
WORDPRESS_EMAIL="${WORDPRESS_EMAIL:-user@example.com}" 
WORDPRESS_FIRST_NAME="${WORDPRESS_FIRST_NAME:-FirtsName}" 
WORDPRESS_LAST_NAME="${WORDPRESS_LAST_NAME:-LastName}" 
WORDPRESS_BLOG_NAME="${WORDPRESS_BLOG_NAME:-Mi Blog}" 
WORDPRESS_SCHEME="${WORDPRESS_SHEME:-http}" 
WORDPRESS_HTACCESS_OVERRIDE_NONE="${WORDPRESS_HTACCESS_OVERRIDE_NONE:-yes}
## GOOGLE_APPLICATION_CREDENTIALS="${GOOGLE_APPLICATION_CREDENTIALS:-/opt/credenciales/credenciales.json}"   
## BUCKET="${BUCKET:-mibucket}"
##CANIJO="${CANIJO:-amable}"
CANIJO1=${CANIJO1:-amable1}